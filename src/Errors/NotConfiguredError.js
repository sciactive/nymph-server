/**
 * NotConfiguredError exception.
 *
 * This exception is thrown when Nymph detects that it hasn't been configured
 * and is being used.
 */
export default class NotConfiguredError extends Error {}
